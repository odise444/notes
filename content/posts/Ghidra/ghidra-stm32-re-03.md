---
title: "Ghidra로 STM32 부트로더 역분석한 썰 #3 - Ghidra 설치와 첫 로드"
date: 2024-12-09
draft: false
tags: ["Ghidra", "STM32", "리버스엔지니어링", "역분석", "ARM", "Cortex-M"]
categories: ["역분석"]
summary: "Ghidra 설치하고 STM32 바이너리를 처음 로드해보자. 설정 하나 잘못하면 분석 망한다."
---

## 지난 글 요약

[지난 글](/posts/ghidra/ghidra-stm32-re-02/)에서 Intel HEX 포맷을 이해하고 BIN으로 변환했다. 이제 Ghidra에 올려보자.

## Ghidra 설치

### 1. JDK 설치

Ghidra는 Java로 만들어졌다. **JDK 17 이상** 필요.

```bash
# Windows: Oracle 또는 Adoptium에서 다운로드
# https://adoptium.net/

# Linux (Ubuntu)
sudo apt install openjdk-17-jdk

# Mac
brew install openjdk@17

# 버전 확인
java -version
# openjdk version "17.0.x" ...
```

### 2. Ghidra 다운로드

공식 사이트: https://ghidra-sre.org/

```bash
# 압축 해제
unzip ghidra_11.0_PUBLIC_20231222.zip

# 실행
cd ghidra_11.0_PUBLIC
./ghidraRun    # Linux/Mac
ghidraRun.bat  # Windows
```

**설치 끝.** 인스톨러 없이 그냥 실행하면 된다.

## 첫 실행 화면

```
┌─────────────────────────────────────┐
│         Ghidra Project Window       │
├─────────────────────────────────────┤
│  File  Edit  Project  Tools  Help   │
├─────────────────────────────────────┤
│                                     │
│   [No Active Project]               │
│                                     │
│   Tool Chest:                       │
│   🐉 CodeBrowser                    │
│   📊 Version Tracking               │
│                                     │
└─────────────────────────────────────┘
```

## 프로젝트 생성

**File → New Project**

```
Project Type: [x] Non-Shared Project
             [ ] Shared Project

→ Next

Project Directory: C:\Ghidra\Projects
Project Name: STM32_Bootloader

→ Finish
```

**Shared Project**는 팀 협업용. 혼자면 Non-Shared.

## 바이너리 Import

**File → Import File** (또는 `I` 키)

`200429.bin` 선택.

### Format 선택

```
┌─────────────────────────────────────┐
│           Import File               │
├─────────────────────────────────────┤
│  Format: [Raw Binary          ▼]   │
│                                     │
│  Language: [                  ▼]   │  ← 중요!
│                                     │
│  [Options...]                       │
└─────────────────────────────────────┘
```

**Format**: `Raw Binary` (자동 감지됨)

### Language 선택 (핵심!)

**Language** 버튼 클릭:

```
┌─────────────────────────────────────────────┐
│              Select Language                │
├─────────────────────────────────────────────┤
│  Filter: ARM                                │
├─────────────────────────────────────────────┤
│  Processor   Variant    Size  Endian        │
│  ─────────────────────────────────────────  │
│  ARM         v7         32    little   ✓    │
│  ARM         v8         32    little        │
│  ARM         Cortex     32    little   ✓✓   │ ← 이거!
│  ARM         v4t        32    little        │
│  ...                                        │
└─────────────────────────────────────────────┘
```

**STM32F103 = Cortex-M3** → `ARM:LE:32:Cortex` 선택

| MCU 시리즈 | 코어 | Ghidra 설정 |
|------------|------|-------------|
| STM32F0 | Cortex-M0 | ARM:LE:32:Cortex |
| STM32F1 | Cortex-M3 | ARM:LE:32:Cortex |
| STM32F4 | Cortex-M4 | ARM:LE:32:Cortex |
| STM32H7 | Cortex-M7 | ARM:LE:32:Cortex |

**LE** = Little Endian (ARM 기본)

### Options 설정 (중요!)

**Options...** 클릭:

```
┌─────────────────────────────────────┐
│         Import Options              │
├─────────────────────────────────────┤
│  Block Name: [ram                 ] │
│  Base Address: [0x08000000        ] │ ← 핵심!
│  File Offset: [0x0                ] │
│  Length: [0x4000 (16384)          ] │
└─────────────────────────────────────┘
```

**Base Address: `0x08000000`**

이거 안 하면 주소가 0x00000000부터 시작해서 분석이 엉망이 된다.

```
잘못된 경우:
0x00000000: Reset_Handler → 0x00002801 (???)

올바른 경우:
0x08000000: Reset_Handler → 0x08002801 (Flash 영역 ✓)
```

## Import 완료

```
┌─────────────────────────────────────┐
│        Import Results Summary       │
├─────────────────────────────────────┤
│  Program Name: 200429.bin           │
│  Language: ARM:LE:32:Cortex         │
│  Compiler: default                  │
│                                     │
│  Bytes Imported: 16384              │
│  Initial Memory Blocks: 1           │
│    ram: 0x08000000 - 0x08003fff     │
└─────────────────────────────────────┘
```

**OK** 클릭.

## 자동 분석 실행

Import 직후 물어본다:

```
┌─────────────────────────────────────┐
│  200429.bin has not been analyzed.  │
│  Would you like to analyze it now?  │
│                                     │
│        [Yes]  [No]  [Analyze...]    │
└─────────────────────────────────────┘
```

**Yes** 또는 **Analyze...** 클릭.

### 분석 옵션

```
┌─────────────────────────────────────────────┐
│            Analysis Options                 │
├─────────────────────────────────────────────┤
│  [x] ARM Aggressive Instruction Finder      │ ← 권장
│  [x] ASCII Strings                          │
│  [x] Create Address Tables                  │
│  [x] Data Reference                         │
│  [x] Decompiler Parameter ID                │ ← 디컴파일 품질 향상
│  [x] Embedded Media                         │
│  [x] Function Start Search                  │
│  [x] Non-Returning Functions - Discovered   │
│  [x] Reference                              │
│  [x] Stack                                  │
│  ...                                        │
└─────────────────────────────────────────────┘
```

기본값으로 OK. ARM 관련 옵션 체크 확인.

**Analyze** 클릭 → 잠시 대기 (16KB면 몇 초)

## CodeBrowser 화면 구성

분석 완료 후 메인 화면:

```
┌─────────────────────────────────────────────────────────────────┐
│  File  Edit  Analysis  Navigation  Search  Select  Tools  ...  │
├──────────────┬──────────────────────────┬──────────────────────┤
│  Program     │      Listing             │    Decompile         │
│  Trees       │   (Disassembly)          │    (C 유사 코드)      │
│              │                          │                      │
│ ▼ Functions  │  08000000 18 52 00 20    │  void FUN_08002800() │
│   FUN_080... │  08000004 01 28 00 08    │  {                   │
│   FUN_080... │  08000008 1f 2b 00 08    │    ...               │
│   ...        │  ...                     │  }                   │
│              │                          │                      │
├──────────────┴──────────────────────────┴──────────────────────┤
│  Console / Bytes / Defined Data / Symbol Table / ...           │
└─────────────────────────────────────────────────────────────────┘
```

### 주요 패널

| 패널 | 용도 |
|------|------|
| Program Trees | 메모리 영역, 함수 목록 |
| Listing | 디스어셈블리 (어셈블리 코드) |
| Decompile | C 유사 코드 (핵심!) |
| Symbol Table | 함수/변수 이름 |
| Bytes | Raw 바이트 뷰 |

## 첫 확인: Vector Table

주소 `0x08000000`으로 이동 (G 키 → `08000000` 입력):

```
                     RAM
08000000 18 52 00 20     addr       DAT_20005218
08000004 01 28 00 08     addr       FUN_08002800+1  ; Reset
08000008 1f 2b 00 08     addr       FUN_08002b1e+1  ; NMI
0800000c ab 20 00 08     addr       FUN_080020aa+1  ; HardFault
08000010 79 20 00 08     addr       FUN_08002078+1  ; MemManage
...
```

**자동 분석이 Vector Table을 인식했다!**

- `0x08000000`: Initial SP = `0x20005218` (SRAM)
- `0x08000004`: Reset_Handler = `0x08002800`

## Reset_Handler 찾아가기

`0x08000004` 더블클릭 또는:

**G 키 → `08002800`**

```asm
                     FUN_08002800
08002800 10 b5           push       { r4, lr }
08002802 04 4c           ldr        r4, [DAT_08002814]
08002804 20 46           mov        r0, r4
08002806 00 f0 4d f8     bl         FUN_080028a4
0800280a 20 46           mov        r0, r4
0800280c 00 f0 72 f8     bl         FUN_080028f4
08002810 10 bd           pop        { r4, pc }
```

오른쪽 **Decompile** 패널:

```c
void FUN_08002800(void)
{
  FUN_080028a4(DAT_08002814);
  FUN_080028f4(DAT_08002814);
  return;
}
```

**이게 main() 또는 SystemInit() 같은 초기화 함수다!**

## 삽질 1: Language 잘못 선택

ARM v7 대신 ARM v4t 선택하면:

```
08002800 ?? ??           invalid
08002802 ?? ??           invalid
```

**Thumb-2 명령어를 해석 못 함.** 

Cortex-M은 Thumb-2 명령어 세트를 쓴다. 반드시 `ARM:LE:32:Cortex` 선택.

## 삽질 2: Base Address 누락

Base Address를 0으로 두면:

```
00000004: Reset = 0x00002801
→ 0x00002800으로 이동
→ 데이터 영역 (Vector Table 중간)
→ 엉뚱한 코드 분석
```

**Import 후에는 수정 어려움.** 처음부터 다시 Import 해야 함.

### 이미 Import 했다면?

**Window → Memory Map** 에서 확인:

```
Name    Start       End         Length
ram     0x00000000  0x00003fff  0x4000
```

0x08000000이 아니면 **다시 Import**.

## 메모리 맵 추가 설정

STM32 분석을 위해 추가 메모리 영역 정의하면 좋다.

**Window → Memory Map → 🏠 (Home) 아이콘**

```
┌─────────────────────────────────────────────┐
│              Add Memory Block               │
├─────────────────────────────────────────────┤
│  Block Name: [SRAM                        ] │
│  Start Addr: [0x20000000                  ] │
│  Length:     [0x10000                     ] │ (64KB)
│  [x] Read  [x] Write  [ ] Execute          │
└─────────────────────────────────────────────┘
```

| 영역 | 시작 | 크기 | R/W/X |
|------|------|------|-------|
| Flash | 0x08000000 | 0x80000 | R/X |
| SRAM | 0x20000000 | 0x10000 | R/W |
| Peripherals | 0x40000000 | 0x30000 | R/W |

주변장치 영역 추가하면 레지스터 접근 분석에 도움.

## 저장

**File → Save** (Ctrl+S)

Ghidra는 프로젝트 폴더에 자체 포맷으로 저장. `.gpr` 파일.

## 정리

| 단계 | 설정 |
|------|------|
| JDK | 17 이상 |
| Language | ARM:LE:32:Cortex |
| Base Address | 0x08000000 (STM32 Flash) |
| 자동 분석 | Yes (기본 옵션 OK) |
| 메모리 맵 | SRAM, Peripherals 추가 권장 |

**다음 글에서**: 디스어셈블리와 디컴파일 차이, 어셈블리 읽는 법.

---

## 시리즈 목차

1. [왜 역분석을 하게 됐나](/posts/ghidra/ghidra-stm32-re-01/)
2. [HEX 파일 구조 이해하기](/posts/ghidra/ghidra-stm32-re-02/)
3. **Ghidra 설치와 첫 로드** ← 현재 글
4. 디스어셈블리 vs 디컴파일
5. Vector Table 분석
6. ...

---

## 단축키 정리

| 키 | 기능 |
|----|------|
| G | Go to address |
| D | Disassemble |
| C | Clear (disassembly 취소) |
| F | Create function |
| L | Label (이름 지정) |
| ; | Comment |
| Ctrl+S | 저장 |

---

## 참고 자료

- [Ghidra 공식 설치 가이드](https://ghidra-sre.org/InstallationGuide.html)
- [Ghidra Cheat Sheet](https://ghidra-sre.org/CheatSheet.html)
- [ARM Cortex-M3 Technical Reference](https://developer.arm.com/documentation/ddi0337/latest)
